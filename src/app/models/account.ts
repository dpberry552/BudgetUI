import { Time } from '@angular/common';

export class Account {
    Id: number;
    UserId: number;
    Description: string;
    Type: string;
    Institution: string;
    Balance: number;
    CreateDate: Date;
    UpdateDate: Date;
}